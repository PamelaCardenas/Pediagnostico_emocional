<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prediagnosticoemocional">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".log_Nino"></activity>
        <activity android:name=".resultadosPadre" />
        <activity android:name=".cuentos" />
        <activity android:name=".DibujoFamiliaNino" />
        <activity android:name=".Registro" />
        <activity android:name=".Actividades_Nino" />
        <activity android:name=".acivity_log_padre" />
        <activity
            android:name=".ui.login.LoginActivityPadre"
            android:label="@string/title_activity_login_padre" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>